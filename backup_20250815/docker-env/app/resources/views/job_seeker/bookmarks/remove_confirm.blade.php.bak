<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="{{ csrf_token() }}">
    <title>ブックマーク削除確認 - 求人応募ポータル</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <!-- ナビゲーション -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-briefcase me-2"></i>求人応募ポータル
            </a>
            
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="{{ route('jobs.show', $job->id) }}">
                    <i class="fas fa-arrow-left me-1"></i>求人詳細に戻る
                </a>
                <a class="nav-link" href="{{ route('job_seeker.dashboard') }}">
                    <i class="fas fa-user me-1"></i>マイページ
                </a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6">
                <!-- ヘッダー -->
                <div class="text-center mb-4">
                    <h1 class="h3 mb-2">
                        <i class="fas fa-exclamation-triangle text-warning me-2"></i>ブックマーク削除確認
                    </h1>
                    <p class="text-muted">以下の求人をブックマークから削除しますか？</p>
                </div>

                <!-- 求人情報カード -->
                <div class="card mb-4 shadow-sm">
                    @if($job->image_url)
                        <img src="{{ $job->image_url }}" class="card-img-top" style="height: 250px; object-fit: cover;" alt="求人画像">
                    @else
                        <div class="card-img-top bg-light d-flex align-items-center justify-content-center" style="height: 250px;">
                            <i class="fas fa-briefcase fa-4x text-muted"></i>
                        </div>
                    @endif
                    
                    <div class="card-body">
                        <h5 class="card-title text-primary">{{ $job->title }}</h5>
                        
                        <div class="row mb-3">
                            <div class="col-12 mb-2">
                                <i class="fas fa-building me-2 text-primary"></i>
                                <strong>企業名:</strong> {{ $job->company->company_name }}
                            </div>
                            <div class="col-12 mb-2">
                                <i class="fas fa-map-marker-alt me-2 text-danger"></i>
                                <strong>勤務地:</strong> {{ $job->location }}
                            </div>
                            <div class="col-12 mb-2">
                                <i class="fas fa-yen-sign me-2 text-success"></i>
                                <strong>給与:</strong> {{ $job->salary_range }}
                            </div>
                            <div class="col-12 mb-2">
                                <i class="fas fa-clock me-2 text-info"></i>
                                <strong>雇用形態:</strong> {{ $job->employment_type }}
                            </div>
                            <div class="col-12">
                                <i class="fas fa-bookmark me-2 text-warning"></i>
                                <strong>ブックマーク日:</strong> 
                                <span class="text-muted">{{ $bookmark->bookmarked_at->format('Y年m月d日 H:i') }}</span>
                            </div>
                        </div>

                        <div class="mb-3">
                            <h6><i class="fas fa-file-alt me-2 text-secondary"></i>仕事内容</h6>
                            <div class="text-muted" style="max-height: 120px; overflow-y: auto;">
                                {{ Str::limit(strip_tags($job->description), 200) }}
                            </div>
                        </div>
                    </div>
                </div>

                <!-- 確認メッセージ -->
                <div class="card border-danger mb-4">
                    <div class="card-body text-center">
                        <div class="mb-3">
                            <i class="fas fa-trash fa-3x text-danger"></i>
                        </div>
                        <h5 class="card-title text-danger">ブックマークを削除</h5>
                        <p class="card-text text-muted mb-0">
                            この求人をブックマークから削除すると、マイページのブックマーク一覧から削除されます。<br>
                            <strong>この操作は取り消せません。</strong>
                        </p>
                    </div>
                </div>

                <!-- アクションボタン -->
                <form method="POST" action="{{ route('job_seeker.bookmarks.destroy', $job->id) }}">
                    @csrf
                    @method('DELETE')
                    <!-- リダイレクト先を指定 -->
                    <input type="hidden" name="redirect_to" value="{{ url()->previous() }}">
                    
                    <div class="d-grid gap-2">
                        <button type="submit" class="btn btn-danger btn-lg" onclick="return confirm('本当にブックマークを削除しますか？')">
                            <i class="fas fa-trash me-2"></i>ブックマークから削除する
                        </button>
                        
                        <div class="row">
                            <div class="col-6">
                                <a href="{{ route('jobs.show', $job->id) }}" class="btn btn-outline-secondary w-100">
                                    <i class="fas fa-arrow-left me-2"></i>求人詳細に戻る
                                </a>
                            </div>
                            <div class="col-6">
                                <a href="{{ route('job_seeker.bookmarks.index') }}" class="btn btn-outline-warning w-100">
                                    <i class="fas fa-list me-2"></i>ブックマーク一覧
                                </a>
                            </div>
                        </div>
                    </div>
                </form>

                <!-- 関連リンク -->
                <div class="text-center mt-4">
                    <div class="row">
                        <div class="col-6">
                            <a href="{{ route('job_seeker.jobs.apply', $job->id) }}" class="btn btn-success w-100">
                                <i class="fas fa-paper-plane me-2"></i>この求人に応募
                            </a>
                        </div>
                        <div class="col-6">
                            <a href="{{ route('jobs.index') }}" class="btn btn-outline-primary w-100">
                                <i class="fas fa-search me-2"></i>求人一覧に戻る
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    
    <style>
    .card {
        transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
    }

    .card:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 16px rgba(0,0,0,0.1);
    }

    .btn {
        transition: all 0.3s ease;
    }

    .btn:hover {
        transform: translateY(-1px);
    }

    .fa-trash {
        animation: shake 2s infinite;
    }

    @keyframes shake {
        0%, 100% {
            transform: translateX(0);
        }
        25% {
            transform: translateX(-2px);
        }
        75% {
            transform: translateX(2px);
        }
    }

    @media (max-width: 768px) {
        .container {
            padding: 0 15px;
        }
        
        .card-body {
            padding: 1rem;
        }
        
        .btn-lg {
            font-size: 1rem;
            padding: 0.75rem 1rem;
        }
    }
    </style>
</body>
</html>